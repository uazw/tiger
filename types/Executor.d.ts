import { StateManager, ModuleRegistry } from "./types";
declare const _default: (path: string, module: string, stateManager: StateManager, moduleRegistry: ModuleRegistry) => (req: Request, res: Response) => void;
export default _default;
